package LibraryManagementSystem.IdGenerator;

public class IDGenerator {

    // main static methods is discuraging but here we know that we will not have multiple states so thats we are going with static methods

    private IDGenerator() {}

    public static String getUniqueId() {
        return "12";
    }
}
